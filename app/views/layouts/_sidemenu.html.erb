<% if logged_in? %>

<%= link_to new_post_path, class:"btn btn-info btn-block" do %>
  <i class="fas fa-pen-fancy"></i>　新規投稿
<% end %>
<%= link_to 'プロフィール', user_path(current_user), class: 'btn btn-light btn-block' %>
<%= link_to 'お気に入り', likes_user_path(current_user), class: 'btn btn-light btn-block' %>

<% end %>
