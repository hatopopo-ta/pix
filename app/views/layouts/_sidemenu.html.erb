<% if logged_in? %>

<a href="<%= new_post_path %>" class="btn btn-info btn-block <%= 'active' if current_page?(new_post_path) %>"><i class="fas fa-pen-fancy"></i>　新規投稿</a>
<a href="<%= user_path(current_user) %>" class="btn btn-light btn-block <%= 'active' if current_page?(user_path(current_user)) %> <%= 'active' if current_page?(followings_user_path(current_user)) %> <%= 'active' if current_page?(followers_user_path(current_user)) %> <%= 'active' if current_page?(followings_user_path(current_user)) %> <%= 'active' if current_page?(edit_user_path(current_user)) %>">プロフィール</a>
<a href="<%= likes_user_path(current_user) %>" class="btn btn-light btn-block <%= 'active' if current_page?(likes_user_path(current_user)) %>">お気に入り</a>

<% end %>
